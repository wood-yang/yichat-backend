# 公共配置文件
spring:
  application:
    name: yupao-backend
  profiles:
    active: prod
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://gz-tdsql-darrkhl7.sql.tencentcdb.com:21056/yupao
    username: wood
    password: 20010309Npy!
  # redis配置
  redis:
    host: r-2vc8otahhn18exw8zhpd.redis.cn-chengdu.rds.aliyuncs.com
    port: 6379
    password: 20010309npy!
#  redis:
#    port: 6379
#    host: localhost
  # session 失效时间（分钟）
  session:
    timeout: 86400
    store-type: redis
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 8080
  servlet:
    context-path: /api
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)